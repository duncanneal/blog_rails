<nav class="top-bar" data-topbar role="navigation" data-options="is_hover: false" data-options="dropdown_autoclose: false">

  <ul class="title-area">
    <li class="name">
      <header><h1><%= link_to 'Cup o Duncan', root_path %></h1></header>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>
<section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="left">
      <li class="has-dropdown">
        <a href="#">Dig in!</a>
        <ul class="dropdown">
          <li><a href="/posts">New Posts</a></li>
          <li><a href="/comments">Comments</a></li>
        </ul>
      </li>
    </ul>
  </section>
  </nav>

  <table>
    <thead>
    <tr>
    <th width="200">Title</th>
      <th>Author</th>
      <th>Date</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
    </thead>

    <tbody>
    <% @posts.each do |post| %>
        <tr>
          <td><%= post.title %></td>
          <td><%= post.author %></td>
          <td><%= post.date %></td>
          <td><%= post.body %></td>
          <td><%= link_to 'Show', post, class: "button tiny" %></td>
          <td><%= link_to 'Edit', edit_post_path(post), class: "button tiny" %></td>
          <td><%= link_to 'Destroy', post, method: :delete, data: {confirm: 'Are you sure?'}, class: "alert button tiny" %></td>
    <% end %>
    </tr>
    </tbody>
  </table>

  
<%= link_to 'Home', root_path, class: "button secondary small" %> 
<%= link_to 'New Post', new_post_path, class: "button secondary small" %>